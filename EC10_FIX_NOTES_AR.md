# PS Cafe Manager V6.4.1-EC10 — Invoice Auto Preview Fix

## الهدف
إظهار الفاتورة تلقائيًا فور إنهاء الجلسة بدل الاكتفاء بإخطار والاضطرار لفتح صفحة الفواتير يدويًا.

## التعديلات
- إصلاح ربط الفاتورة بالجلسة داخل بيانات الواجهة بإضافة `sessionId` من `session_id`.
- بعد إنهاء الجلسة سحابيًا يتم البحث عن الفاتورة بالـ invoice id أولًا، ثم session id، ثم آخر فاتورة كـ fallback.
- فتح نافذة الفاتورة تلقائيًا بعد نجاح إنشاء الفاتورة.
- في حالة fallback النادر، يتم فتح صفحة الفواتير بدل ترك المستخدم بدون توجيه.
- تحديث رقم النسخة والكاش إلى EC10.

## لا توجد تغييرات
- لا تغيير في Supabase schema.
- لا تغيير في الصلاحيات.
- لا تغيير في منطق الأسعار.
