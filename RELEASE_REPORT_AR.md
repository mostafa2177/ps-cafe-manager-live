# تقرير إصدار PS Cafe Manager — V6.4.1 EC5

## نوع الإصدار

**Cloud Execution Candidate — غير مصرح بالتنفيذ أو النشر بعد.**

- النسخة المنشورة المستقرة: `V6.3.0`.
- نقطة المراجعة المحلية الناجحة: `V6.4.1 R4` للفواتير والصلاحيات وسجل الحركات.
- هذه الحزمة: تحويل منطق R4 إلى قاعدة بيانات Supabase آمنة خاصة بصاحب النشاط مع إصلاحات إضافية.

## القرارات التشغيلية المثبتة

- الكاشير يستطيع تشغيل الجلسات والمباريات وإضافة وتعديل المنتجات والمخزون.
- الكاشير لا يستطيع تصحيح أو إلغاء فاتورة مكتملة.
- Owner يستطيع تصحيح أو إلغاء الفاتورة مع ملاحظة اختيارية؛ ويتم حفظ العملية والوقت والمستخدم في السجل.
- Manager يستطيع تصحيح الفواتير فقط إذا منحه Owner إذنًا صريحًا `can_correct_invoices`.
- الفواتير الملغاة لا تُحذف، وتظل ظاهرة للمراجعة ولا تدخل في الإيراد أو الربح.

## قاعدة البيانات السحابية

المخطط الجديد يحتوي على جداول منفصلة لـ:

- النشاط والأعضاء والبيانات التعريفية.
- الأجهزة والمنتجات.
- الجلسات ومشروبات الجلسة.
- الفواتير وبنودها ومراجعات/تصحيحات الفاتورة.
- المصروفات.
- سجل الحركات.

يتم استخدام Supabase Auth وRLS، ولا تُمنح عمليات الكتابة التشغيلية المباشرة للجداول من التطبيق؛ بل تتم عن طريق RPC محددة تسجل الحدث في قاعدة البيانات.

## إصلاحات EC5 بعد اختبار R4

1. **الفاتورة الملغاة نهائية في SQL:** لا يمكن تصحيحها أو إعادتها لمكتملة من خلال تجاوز الواجهة.
2. **حماية آخر Owner فعال:** يمنع النظام تعطيل أو تخفيض آخر حساب Owner فعال.
3. **إلغاء المصروفات الموثق:** نافذة داخل التطبيق بدل نافذة المتصفح، مع إبقاء المصروف الملغى ظاهرًا وعدم احتسابه في التقارير.
4. **تاريخ تقرير اليوم:** استخدام توقيت مصر بدل تاريخ UTC لتجنب نقل العمليات لليوم الخطأ بعد منتصف الليل.
5. **اتساق الربط السحابي:** تثبيت Schema marker على `normalized_v2_controlled_corrections`.

## ما لم يُنفذ بعد

- لم يتم تشغيل أي SQL على مشروع Supabase الخاص بالمالك.
- لم يتم ربط Project URL أو Publishable Key داخل النسخة المنشورة.
- لم يتم رفع هذا الإصدار إلى GitHub Pages.
- لا توجد بيانات تشغيل حقيقية في هذا الإصدار.

## قرار التنفيذ

قبل الضغط على Run في Supabase، يجب اختبار واجهة المصروفات في هذه الحزمة محليًا، ثم اعتماد ملف SQL النهائي خطوة بخطوة.


## تعديلات EC5 قبل Supabase
- إيقاف الاعتماد على عداد انتهاء المباراة في الواجهة؛ المباراة الآن تسعير ثابت حسب اختيارات مثل 5 دقائق / 10 دقائق.
- إمكانية إضافة مباراة أخرى على جلسة مفتوحة مثل إضافة مشروب، وتظهر كبند داخل الجلسة والفاتورة.
- تحويل إجراءات الفواتير إلى قائمة منبثقة مرتبة بدل أزرار كبيرة داخل الجدول.
- جعل تحرير بنود الفاتورة وأسعار المباريات أكثر compact ومرونة.
- إضافة RPC سحابية `ps_add_custom_session_item` لإضافة مباراة أو بند مخصص للجلسة قبل إغلاقها.


## تعديل EC5 بعد ملاحظة التشغيل

- أصبحت المباراة الإضافية تُضاف إلى رسوم اللعب والمباريات، ولا تُحسب ضمن المشروبات أو الإضافات.
- تم فصل إجمالي المشروبات عن إجمالي المباريات في الفاتورة والتقرير.
- تم حفظ نوع البند `match` في الواجهة وملف Supabase حتى تُجمع البنود بطريقة صحيحة بعد الربط السحابي.


## تعديل EC5 بعد ملاحظة التشغيل

- إضافة توضيح عدد المباريات في كارت الجلسة المفتوحة.
- إضافة عدد المباريات في نافذة الفاتورة والطباعة.
- إضافة عمود عدد المباريات في قائمة الفواتير.
- التقرير اليومي يستمر في احتساب المباراة الأساسية + أي مباريات إضافية.


## تحديث EC5
- ملاحظة التصحيح أو الإلغاء أصبحت اختيارية في الفواتير والمصروفات، مع استمرار تسجيل العملية والوقت والمستخدم في سجل الحركات.
