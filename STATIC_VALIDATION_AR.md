# تقرير الفحص الساكن — V6.4.1 EC5

## النتيجة

تم إجراء فحص ساكن للحزمة بعد تعديلات EC5. هذا الفحص لا يساوي اختبار Supabase فعليًا ولا يصرح بالتنفيذ على قاعدة المالك.

## فحوص نجحت

- تحليل صياغة ملف SQL بنجاح باستخدام PostgreSQL parser (`pglast`).
- فحص JavaScript syntax لملف `index.html` بنجاح باستخدام `node --check`.
- تحقق وجود حماية تمنع تعديل الفاتورة بعد تحولها إلى `voided`.
- تحقق وجود حماية تمنع فقد آخر Owner فعال.
- تحقق وجود واجهة إلغاء مصروف موثّق داخل التطبيق بدل `prompt`.
- تحقق أن تقرير اليوم يستبعد الفواتير والمصروفات الملغاة.
- تحقق أن تقرير اليوم يستخدم توقيت `Africa/Cairo`.
- تحقق اتساق ربط التطبيق مع schema marker الجديد `normalized_v2_controlled_corrections`.
- تحقق أن دوال المساعدة الحساسة في schema `private` وليست داخل API العام.
- تحقق أن ملفات PWA ما زالت تحمل هوية الشخصية المعتمدة.

## يحتاج اختبارًا يدويًا قبل Supabase

- إضافة وإلغاء مصروف تجريبي في الواجهة المحلية والتأكد من العرض والتقرير وسجل الحركات.

## لم يتم

- لم يُشغل SQL في Supabase.
- لم تُنفذ اختبارات RLS بحساب Owner/Cashier فعليين.
- لم تُرفع الملفات إلى GitHub.


## فحص EC5 الإضافي

- تم فحص JavaScript بعد تعديل فصل بنود المباراة عن المشروبات.
- تم تحديث SQL لإضافة `item_type` إلى بنود الجلسة والفاتورة، وتجميع `match` داخل `play_total` و`drink` داخل `drink_total`.
